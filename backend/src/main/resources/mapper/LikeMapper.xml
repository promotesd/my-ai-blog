<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaodudu.blog.mapper.LikeMapper">
  <select id="exists" resultType="int">
    SELECT COUNT(1) FROM like_record
    WHERE target_type = #{targetType} AND target_id = #{targetId} AND client_key = #{clientKey}
  </select>

  <insert id="insert">
    INSERT INTO like_record (target_type, target_id, client_key)
    VALUES (#{targetType}, #{targetId}, #{clientKey})
  </insert>

  <select id="count" resultType="int">
    SELECT COUNT(1) FROM like_record
    WHERE target_type = #{targetType} AND target_id = #{targetId}
  </select>
</mapper>

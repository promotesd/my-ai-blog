<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaodudu.blog.mapper.CommentMapper">
  <select id="findAll" resultType="com.xiaodudu.blog.entity.Comment">
    SELECT * FROM comment ORDER BY created_at DESC
  </select>
  <select id="findApproved" resultType="com.xiaodudu.blog.entity.Comment">
    SELECT * FROM comment
    WHERE target_type = #{targetType}
      AND target_id = #{targetId}
      AND status = 'approved'
    ORDER BY created_at DESC
  </select>

  <insert id="insert" parameterType="com.xiaodudu.blog.entity.Comment" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO comment (post_id, target_type, target_id, nickname, email, content, status)
    VALUES (#{postId}, #{targetType}, #{targetId}, #{nickname}, #{email}, #{content}, #{status})
  </insert>
  <update id="updateStatus">
    UPDATE comment SET status = #{status} WHERE id = #{id}
  </update>
  <delete id="deleteById">
    DELETE FROM comment WHERE id = #{id}
  </delete>
</mapper>
